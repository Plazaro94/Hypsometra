# workers

Procesos de cómputo para optimización, walk-forward y Monte Carlo.

Vercel sirve el dashboard (`apps/web`). Aquí viven los workers que pueden
usar CPU/RAM de forma sostenida (máquina local o cloud dedicado).

Estado: reservado. Sin runtime todavía.
